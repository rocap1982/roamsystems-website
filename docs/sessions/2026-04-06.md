# Session — 2026-04-06

## Summary

- Fixed Stripe price mismatches: 3 products had stale amounts (M1 Standard, M1 Full Width, Aluminium Panels)
- Fixed `tax_behavior` on ALL 9 Stripe prices — was `unspecified` (treated as tax-inclusive), now `exclusive`
- Both issues caused customers to be undercharged at Stripe checkout vs what the website displayed
- Fixed shipping rate `tax_behavior` — was `inclusive`, now `exclusive` (£130 + VAT, not £130 inc VAT)

## Closed / shipped

- Created 9 new Stripe prices with correct amounts and `tax_behavior=exclusive`
- Archived 12 old Stripe prices (3 stale amounts + 9 without tax_behavior)
- Updated `src/data/products.json` with final price IDs (2 commits: `49d65a4`, `1fd6ee8`)
- Both deployed to Railway successfully
- Created new shipping rate `shr_1TJ8LvP8jCOedj2eZrqDCFcY` with `tax_behavior=exclusive`, archived old one
- Updated `STRIPE_SHIPPING_RATE_ID` env var in Railway (no code change needed)

## Decisions / notes

- Stripe prices are immutable — must create new + archive old for any change
- `automatic_tax: { enabled: true }` in checkout requires `tax_behavior=exclusive` on all prices
- Shipping rate also needed `tax_behavior=exclusive` — £130 is ex-VAT like all other prices
- Saved feedback memory for future sessions: always set `tax_behavior=exclusive` and verify checkout totals

## Next / blockers

- None — all prices verified and deployed
