# Session — 2026-07-02

Workflow: small fix (1 data file + 1 canonical doc). No sprint.

## Summary

- Aligned the product catalogue to a single canonical **`roam_code`** shared across Stripe (`metadata.roam_code` / price `lookup_key`) and Sage (`item_code`). Added `roam_code` to all 9 products in `src/data/products.json`.
- Wired the two previously enquiry-only Upholstery products to Stripe checkout:
  - Full Upholstery Kit → `price_1ToN5eP8jCOedj2egdLwuX7B`
  - Cushion Boards & Dual Density Foam → `price_1ToN5fP8jCOedj2enR5j7Vp5`
- Flipped both from `madeToOrder: true` → `false` — the product/listing templates branch on `madeToOrder`, so `true` rendered a "Send Enquiry" modal (the old "no buy path"). `false` renders the standard Add-to-Basket / Stripe flow like the other 7.

## Closed / shipped

- Commit `384725e` on branch `feat/roam-code-catalogue-alignment`, pushed to `origin`. Not yet merged / no PR opened.
- Updated canonical `docs/reference/SCHEMA_AND_CONTRACTS_CANONICAL.md`: documented the `roam_code` field + uniqueness constraint, added the full roam_code → website id → Stripe price mapping table, and a note on the two products' switch to checkout.

## Decisions / notes

- Field key is literal `roam_code` (not camelCase `roamCode`) to keep the key identical across Stripe/Sage/website for reconciliation. Deviates from the file's camelCase convention by intent.
- No displayed `price` values changed. All prices net (ex-VAT), `tax_behavior=exclusive`.
- Checkout API (`/api/checkout`) has no price-ID allowlist — passes IDs straight to Stripe, so no server change needed.
- Side effect (intended): Google Merchant feed derives `availability` from `madeToOrder`, so the two upholstery items now report `in_stock` instead of `preorder`.
- Verified: `node` mapping check (all 9 correct), `npm run build` clean, built HTML for both upholstery pages shows `add-to-cart` button with correct price ID and **no** enquiry button element.

## Next / blockers

- Owner to decide: open PR / merge `feat/roam-code-catalogue-alignment` and deploy.
- Open question: the two upholstery items are bespoke (100+ fabric choices) but checkout captures no fabric selection. Fine if handled via post-order enquiry; otherwise a follow-up change (checkout custom field or post-order flow) is needed.
